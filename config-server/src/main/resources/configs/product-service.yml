server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/product_db
    username: admin
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs